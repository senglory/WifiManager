<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:WiFiManager"
             x:Class="WiFiManager.MainPage">

    <ContentPage.Resources>

        <!-- Default DataTemplate -->
        <DataTemplate x:Key="DefaultDataTemplate">
        </DataTemplate>
        <DataTemplate x:Key="NoteDataTemplate">
            <ViewCell>
                <StackLayout Orientation="Vertical"  HorizontalOptions="Fill">
                    <Label Text="{Binding Name}" />
                    <Label Text="{Binding BssID}" />
                </StackLayout>
            </ViewCell>
        </DataTemplate>
        <DataTemplate x:Key="EditNoteDataTemplate">
            <ViewCell>
                <StackLayout Orientation="Vertical"  HorizontalOptions="Fill">
                    <Entry Text="{Binding Name}" />
                    <Entry Text="{Binding BssID}" />
                </StackLayout>
            </ViewCell>
        </DataTemplate>

        <local:ItemTemplateSelector x:Key="viewInListTemplateSelector"
              DefaultnDataTemplate="{StaticResource DefaultDataTemplate}"
              NoteDataTemplate="{StaticResource NoteDataTemplate}" 
                                        />
        <local:ItemTemplateSelector x:Key="editTemplateSelector"
              DefaultnDataTemplate="{StaticResource DefaultDataTemplate}"
              NoteDataTemplate="{StaticResource EditNoteDataTemplate}" 
                                        />

    </ContentPage.Resources>
    <StackLayout>
        <Button Text="Save" Command="{Binding SaveCommand}"  ></Button>
        <ListView
            VerticalOptions="FillAndExpand" 
            ItemsSource="{Binding WifiNetworks}"
            SelectedItem="{Binding SelectedNetwork}"
            ItemTemplate="{StaticResource viewInListTemplateSelector}"
            ItemSelected="ListView_ItemSelected"
            >
            <!-- The behavior that allows to use a command for the SelectedItem event -->
            <!--<behaviorsPack:SelectedItemBehavior Command="{Binding ItemSelectedCommand}" />-->
            <!--<ListView.Behaviors>
            </ListView.Behaviors>-->
        </ListView>
        <StackLayout x:Name="DetailsArea">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="40" />
                </Grid.RowDefinitions>
                <StackLayout Grid.Row="0" HorizontalOptions="FillAndExpand" Padding="5">
                    <Label Text="{Binding Name}"></Label>
                    <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand" Padding="5">
                        <Label Text="Password"></Label>
                        <Entry Text="{Binding Password}" WidthRequest="100" MinimumWidthRequest="75"></Entry>
                    </StackLayout>
                </StackLayout>
                <ListView Grid.Row="1" x:Name="lstCoords" ItemsSource="{Binding CoordsAndPower}" VerticalOptions="FillAndExpand" >
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <ViewCell>
                                    <StackLayout Orientation="Horizontal" HorizontalOptions="Fill" BackgroundColor="Ivory">
                                        <Label Text="{Binding Long}" BackgroundColor="AliceBlue"></Label>
                                        <Label Text="{Binding Lat}"></Label>
                                        <Label Text="{Binding Power}"></Label>
                                    </StackLayout>
                                </ViewCell>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                <Button Grid.Row="2" Text="Refresh Coords" Clicked="RefreshCoords_Clicked" />
            </Grid>
        </StackLayout>
    </StackLayout>

</ContentPage>

<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:WiFiManager"
             xmlns:shapes="clr-namespace:Stormlion.ShapeControl;assembly=Stormlion.ShapeControl"
             x:Class="WiFiManager.MainPage"
             x:Name="MainPageMain">

    <ContentPage.Resources>

        <local:GreaterThan x:Key="GreaterThanCvt"  />
        
        <!-- Default DataTemplate -->
        <DataTemplate x:Key="DefaultDataTemplate">
        </DataTemplate>
        <DataTemplate x:Key="NoteDataTemplate">
            <ViewCell>
                <StackLayout Orientation="Vertical"  HorizontalOptions="Fill" >
                    <StackLayout.Resources>
                        <Style TargetType="StackLayout">
                            <Style.Triggers>
                                <DataTrigger TargetType="StackLayout" Binding="{Binding IsInCSVList}" Value="True">
                                    <Setter Property="BackgroundColor" Value="Gray" />
                                </DataTrigger>
                                <DataTrigger TargetType="StackLayout" Binding="{Binding IsSelected}" Value="True">
                                    <Setter Property="BackgroundColor" Value="OrangeRed" />
                                </DataTrigger>
                                <DataTrigger TargetType="StackLayout" Binding="{Binding IsEnabled}" Value="False">
                                    <Setter Property="BackgroundColor" Value="DarkRed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackLayout.Resources>
                    <Grid RowSpacing="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="25"/>
                            <RowDefinition Height="25"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Row="0" Grid.Column="0" Text="{Binding Name}"  HeightRequest="20" />
                        <Label Grid.Row="1" Grid.Column="0" Text="{Binding BssID}"  HeightRequest="20" />
                        <BoxView Grid.RowSpan="2" Grid.Column="1" WidthRequest="10"  >
                            <BoxView.Style>
                                <Style TargetType="BoxView">
                                    <Style.Triggers >
                                        <DataTrigger TargetType="BoxView" Binding="{Binding Level, Converter={StaticResource GreaterThanCvt}, ConverterParameter=0}" Value="True">
                                            <Setter Property="BackgroundColor" Value="Green" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </BoxView.Style>
                        </BoxView>
                    </Grid>
                </StackLayout>
            </ViewCell>
        </DataTemplate>
        <DataTemplate x:Key="EditNoteDataTemplate">
            <ViewCell>
                <StackLayout Orientation="Vertical"  HorizontalOptions="Fill">
                    <Entry Text="{Binding Name}" />
                    <Entry Text="{Binding BssID}" />
                </StackLayout>
            </ViewCell>
        </DataTemplate>

        <local:ItemTemplateSelector x:Key="viewInListTemplateSelector"
              DefaultnDataTemplate="{StaticResource DefaultDataTemplate}"
              NoteDataTemplate="{StaticResource NoteDataTemplate}" 
                                        />

        <ResourceDictionary>
            <Style TargetType="Label">
                <Setter Property="TextColor" Value="LightGreen" />
                <Setter Property="HeightRequest" Value="27"/>
            </Style>
            <Style TargetType="Entry">
                <Setter Property="TextColor" Value="LightGreen" />
            </Style>
            <Style TargetType="Button">
                <Setter Property="TextColor" Value="LightGreen" />
                <Setter Property="BackgroundColor" Value="Black" />
                <Setter Property="BorderColor" Value="Beige" />
                <Setter Property="BorderWidth" Value="1" />
                <Style.Triggers>
                    <EventTrigger Event="Pressed">
                        <local:ButtonTriggerAction BackgroundColor="Maroon" />
                    </EventTrigger>
                    <EventTrigger Event="Released">
                        <local:ButtonTriggerAction BackgroundColor="Black" />
                    </EventTrigger>
                </Style.Triggers>
            </Style>
            <Style TargetType="StackLayout">
                <Setter Property="BackgroundColor" Value="Black" />
            </Style>
            <Style TargetType="ListView">
                <Setter Property="BackgroundColor" Value="Black" />
            </Style>


        </ResourceDictionary>
    </ContentPage.Resources>
    <StackLayout>
        <ActivityIndicator x:Name="pleaseWait"
                           IsRunning="False"
                           IsVisible="False"
                           VerticalOptions="Center" 
                           BackgroundColor="Black"
                           />
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <RowDefinition Height="200" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackLayout Grid.Row="0" HorizontalOptions="Fill" Orientation="Horizontal">
                <Button Text="Save" Command="{Binding SaveCommand}" ></Button>
                <Button Text="Refresh" Command="{Binding RefreshNetworksCommand}" ></Button>
            </StackLayout>

                <ListView
                    x:Name="lstNetworks"
                    Grid.Row="1"
                    SeparatorColor="Beige"
                    ItemsSource="{Binding  WifiNetworks}"
                    SelectedItem="{Binding SelectedNetwork}"
                    ItemTemplate="{StaticResource viewInListTemplateSelector}"
                    >
                </ListView>

            <StackLayout Grid.Row="2" x:Name="DetailsArea" >
                <Frame HasShadow="False" Padding="1" >
                    <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand" VerticalOptions="Start" BindingContext="{Binding  SelectedItem,Source={x:Reference lstNetworks}}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition Height="40" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackLayout Grid.Row="0" HorizontalOptions="FillAndExpand">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="30" />
                                        <RowDefinition Height="40" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>
                                    <Label Grid.Row="0" Grid.Column="0" Text="{Binding Name}"></Label>
                                    <Switch Grid.Row="0" Grid.Column="1"  x:Name="ctlIsEnabled" IsToggled="{Binding Path=IsEnabled}" />
                                    <Label Grid.Row="1" Grid.Column="0" Text="Password"></Label>
                                    <Entry Grid.Row="1" Grid.Column="1" Text="{Binding Password}" WidthRequest="100" IsVisible="False" ></Entry>
                                </Grid>
                            </StackLayout>
                            <ListView Grid.Row="1" x:Name="lstCoords" ItemsSource="{Binding CoordsAndPower}"  RowHeight="25" >
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <ViewCell>
                                            <StackLayout Orientation="Horizontal" HorizontalOptions="Fill" BackgroundColor="DarkGray"  >
                                                <Label Text="{Binding Lat}"></Label>
                                                <Label Text="{Binding Long}"></Label>
                                                <Label Text="{Binding Power}"></Label>
                                            </StackLayout>
                                        </ViewCell>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                            <StackLayout Grid.Row="2" Orientation="Horizontal" HorizontalOptions="Fill">
                                <Button Text="Refresh Coords" Clicked="RefreshCoords_Clicked"  />
                                <Button Text="Conn/Disconn" Clicked="ConnDisconn_Clicked" BindingContext="{x:Reference Name=ctlIsEnabled}"  IsEnabled="{Binding Path=IsToggled}"/>
                            </StackLayout>
                        </Grid>
                    </StackLayout>
                    <Frame.Style>
                        <Style TargetType="Frame">
                            <Setter Property="BorderColor" Value="Maroon"></Setter>
                            <Style.Triggers>
                                <DataTrigger TargetType="Frame"  Binding="{Binding   IsConnected}" Value="True" >
                                    <Setter Property="BorderColor" Value="Lime" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Frame.Style>
                </Frame>
            </StackLayout>
        </Grid>
    </StackLayout>

</ContentPage>
